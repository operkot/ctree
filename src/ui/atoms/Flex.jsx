import styled from 'styled-components'
import { flexboxes, space, sizing, borders } from '@xstyled/styled-components'

export const Flex = styled.div(
  { display: 'flex' },
  flexboxes,
  space,
  sizing,
  borders
)
